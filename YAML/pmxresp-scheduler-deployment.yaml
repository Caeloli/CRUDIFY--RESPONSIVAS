apiVersion: apps/v1
kind: Deployment
metadata:
  name: pmxresp-scheduler-deployment
  namespace: ns-gsd-spar-qa01
  labels:
    app: pmxresp-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pmxresp-scheduler
  template:
    metadata:
      labels:
        app: pmxresp-scheduler
    spec:
      containers:
      - name: pmxresp-scheduler
        image: harbor01.un.pemex.com/spar-qa/scheduler-pmxresp:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "0.5"
            memory: "512Mi"
          requests:
            cpu: "0.1"
            memory: "256Mi"
